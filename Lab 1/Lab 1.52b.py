a = 20 # assigns value to a 
b = 5 # assigns value to b
def find_min(a,b): # defines find_min(a,b) as a function
    if a < b: # tells it to test a condition
        return a # tells it to give back a if condtion is true
    else: # says if condtion is not true then...
        return b #return the value of b
    

print (find_min(a,b)) # this code will still work for numbers that are equal